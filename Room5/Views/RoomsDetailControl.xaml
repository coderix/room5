<UserControl
    x:Class="Room5.Views.RoomsDetailControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:telerikGrid="using:Telerik.UI.Xaml.Controls.Grid"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    <ScrollViewer
        Name="ForegroundElement"
        Padding="{StaticResource DetailPageMargin}"
        HorizontalAlignment="Stretch"
        Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
        VerticalScrollMode="Enabled">
        <StackPanel>
            <StackPanel Name="block" Padding="0,0,0,0">
                
               
                <TextBlock Style="{StaticResource DetailBodyBaseMediumStyle}" Text="{x:Bind MasterMenuItem.RoomName, Mode=OneWay}" />
            </StackPanel>

            <telerikGrid:RadDataGrid ItemsSource="{x:Bind BookingRows}"
                                     UserEditMode="None"
                                     x:Name="grid"  AutoGenerateColumns="False"
                                     UserGroupMode="Disabled"
                                     VerticalAlignment="Center"
                                     Width="300" UserFilterMode="Disabled">
                <telerikGrid:RadDataGrid.Columns>
                    <telerikGrid:DataGridTextColumn PropertyName="LessonNumber" Header="Stunde">
                        <telerikGrid:DataGridTextColumn.CellContentStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="Red"/>
                                <Setter Property="FontStyle" Value="Italic"/>
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Style>
                        </telerikGrid:DataGridTextColumn.CellContentStyle>
                    </telerikGrid:DataGridTextColumn>
                    <telerikGrid:DataGridTextColumn PropertyName="Monday.Info" Header="Montag" />
                </telerikGrid:RadDataGrid.Columns>
            </telerikGrid:RadDataGrid>

            <controls:DataGrid x:Name="dataGrid"
                               ItemsSource="{x:Bind BookingRows}"
                                AutoGenerateColumns="False"
                               HeadersVisibility="All"
                               >
                <controls:DataGrid.Columns>
                    <controls:DataGridTextColumn
                        Header="Stunde"
                        Binding="{Binding LessonNumber}"/>
                    <controls:DataGridTextColumn
                        Header="Montag"
                        Binding="{Binding Monday.Info}"/>
                </controls:DataGrid.Columns>

            </controls:DataGrid>

        </StackPanel>
    </ScrollViewer>
</UserControl>
