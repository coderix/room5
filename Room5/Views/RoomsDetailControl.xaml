<UserControl
    x:Class="Room5.Views.RoomsDetailControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:telerikGrid="using:Telerik.UI.Xaml.Controls.Grid"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    <ScrollViewer
        Name="ForegroundElement"
        Padding="{StaticResource DetailPageMargin}"
        HorizontalAlignment="Stretch"
        Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
        VerticalScrollMode="Enabled">
        <StackPanel>
          


             <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="{x:Bind MasterMenuItem.RoomName, Mode=OneWay}" />
            <StackPanel Orientation="Horizontal">
                <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Buchung" />
                <TextBox Text="{x:Bind SelectedBooking.DayOutput,  Mode=OneWay}"/>
            </StackPanel>
           
            <StackPanel>
                
                
                <TextBox Header ="Info" Margin="0,24,0,0" MaxLength="32" Width="400" HorizontalAlignment="Left" Text="{x:Bind SelectedBooking.Info, Mode=TwoWay}" />

                <StackPanel Orientation="Horizontal">
                    <Button Content="OK" Margin="0,24" Click="{x:Bind SaveButtonClicked}"/>
                    <Button Content="Abbruch" Margin="24" Click="CancelButtonClicked"/>
                </StackPanel>

            </StackPanel>

            <telerikGrid:RadDataGrid ItemsSource="{x:Bind BookingRows}"
                                     UserEditMode="None"
                                     x:Name="bookingsGrid"  AutoGenerateColumns="False"
                                     UserGroupMode="Disabled"
                                     VerticalAlignment="Center"
                                     Width="600" UserFilterMode="Disabled" SelectionUnit="Cell"  Tapped="GridTapped"
                                     >
                <telerikGrid:RadDataGrid.Columns>
                    <telerikGrid:DataGridTextColumn PropertyName="LessonNumber" Header="Stunde">
                        <telerikGrid:DataGridTextColumn.CellContentStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="Red"/>
                                <Setter Property="FontStyle" Value="Italic"/>
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Style>
                        </telerikGrid:DataGridTextColumn.CellContentStyle>
                    </telerikGrid:DataGridTextColumn>
                    <telerikGrid:DataGridTextColumn PropertyName="Monday.Info" Header="Montag"/>
                    <telerikGrid:DataGridTextColumn PropertyName="Tuesday.Info" Header="Dienstag"/>
                    <telerikGrid:DataGridTextColumn PropertyName="Wednesday.Info" Header="Mittwoch"/>
                    <telerikGrid:DataGridTextColumn PropertyName="Thursday.Info" Header="Donnerstag"/>
                    <telerikGrid:DataGridTextColumn PropertyName="Friday.Info" Header="Freitag"/>
                    <telerikGrid:DataGridTextColumn PropertyName="Saturday.Info" Header="Samstag" IsVisible="TRue"/>
                    <telerikGrid:DataGridTextColumn PropertyName="Sunday.Info" Header="Sonntag"
                                                    IsVisible="True"/>
                </telerikGrid:RadDataGrid.Columns>
            </telerikGrid:RadDataGrid>

            
        </StackPanel>
    </ScrollViewer>
</UserControl>
